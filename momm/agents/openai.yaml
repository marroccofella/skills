interface:
  display_name: "MOMM (Mixture of Model Modality)"
  short_description: "OAuth-only peer review across six local AI CLIs"
  default_prompt: "Use $momm with this harness as the sole-writer governor, collect OAuth-only peer evidence, then complete every report-bound raw finding-claim and suggestion decision through the structured draft, finalize, and fresh status gates while relaying each required_user_message verbatim."
