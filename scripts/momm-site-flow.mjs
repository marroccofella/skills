import {steps as workflowSteps,governors,scenarios,prompt,routes as reviewerRoutes,fixture} from '../docs/momm/workflow-data.mjs';
const esc=x=>String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function homeWorkflow(){
 const escapedFields=row=>Object.fromEntries(Object.entries(row).map(([key,value])=>[key,typeof value==='string'?esc(value):value]));
 const steps=workflowSteps.map(escapedFields),routes=reviewerRoutes.map(escapedFields);
 const f=fixture();
 return `<section id="architecture-library" class="home-diagrams workflow" aria-labelledby="workflow-heading">
 <div class="wf-heading"><div><p class="eyebrow">FOLLOW THE WORK · MOMM, PRONOUNCED “MOM”</p><h2 id="workflow-heading">From your prompt.<br><span>To proof you can inspect.</span></h2></div><p>One agent holds the pen. Other models challenge the work. Follow the whole loop—and see what changes when a claim has to survive a test.</p></div>
 <div class="wf-demo-label"><span class="wf-indicator" aria-hidden="true"></span> Interactive teaching example · runs only in your browser · no model calls</div>
 <div class="wf-prompt"><span class="wf-avatar" aria-hidden="true">YOU</span><div><span class="wf-kicker">THE SAME PROMPT IN BOTH EXAMPLES</span><p>${esc(prompt)}</p></div></div>
 <div class="wf-controls" data-wf-controls hidden><label>Governor <select data-wf-governor>${governors.map(g=>`<option>${esc(g)}</option>`).join('')}</select></label><label>Explore a path <select data-wf-scenario>${Object.entries(scenarios).map(([v,label])=>`<option value="${v}">${esc(label)}</option>`).join('')}</select></label><div class="wf-playback"><button type="button" data-wf-play aria-pressed="false">▶ Play the workflow</button><button type="button" data-wf-back aria-label="Previous workflow step">←</button><button type="button" data-wf-next aria-label="Next workflow step">→</button><button type="button" data-wf-reset>Restart</button></div></div>
 <figure class="wf-map" aria-labelledby="wf-map-caption"><figcaption id="wf-map-caption">THE END-TO-END FLOW <span>Choose a step to inspect its responsibility.</span></figcaption><ol class="wf-nodes">${steps.map((s,i)=>`<li data-wf-node="${s.id}"><button type="button" data-wf-step="${i}" disabled${i===0?' aria-current="step"':''}><span class="wf-number">${String(i+1).padStart(2,'0')}</span><span><strong>${s.title}</strong><small>${s.short}</small></span><span class="wf-node-arrow" aria-hidden="true">${i===8?'✓':'→'}</span></button></li>`).join('')}</ol><div class="wf-return"><span aria-hidden="true">↶</span> Changed source? Return to the relevant review and verification gates. Old evidence does not automatically cover new code.</div></figure>
 <div class="wf-live" data-wf-live hidden><div class="wf-detail"><div class="wf-detail-top"><span class="wf-kicker" data-wf-actor>You</span><span data-wf-count>01 / 09</span></div><h3 data-wf-title>${steps[0].title}</h3><p data-wf-detail>${steps[0].detail}</p><div class="wf-example" data-wf-example>${steps[0].example}</div><p class="wf-boundary">Your machine coordinates the work. Selected review material crosses to external model providers.</p></div><aside class="wf-reviewers" aria-label="Illustrative reviewer panel"><div class="wf-detail-top"><span class="wf-kicker">READ-ONLY REVIEW TEAM</span><span data-wf-quorum>0 / 2 required</span></div><div data-wf-peers></div><p>Default persona angles, not capability or correctness guarantees. The active governor is excluded.</p></aside></div>
 <p class="wf-announcement" role="status" aria-live="polite" aria-atomic="true" data-wf-announcement></p>
 <div class="wf-comparison"><div class="wf-compare-heading"><p class="eyebrow">SAME TASK. A DIFFERENT EVIDENCE TRAIL.</p><h3>What reaches you at the end?</h3><p>Illustrative outputs—not a live model benchmark. An agent without MOMM can also test and fix code; this example deliberately starts with an unchecked, buggy draft.</p></div><article class="wf-without"><span class="wf-kicker">WITHOUT MOMM · UNCHECKED DRAFT</span><h4>“Done” is not the same as demonstrated.</h4><pre><code>let total = 0;
for (let i = 0; i &lt;= prices.length; i++) {
  total += prices[i];
}
return total;</code></pre><div class="wf-result wf-result-warning">Actual fixture output: <strong>${esc(f.draft)}</strong><br>Expected: ${f.expected} · test fails</div><p>No external review or recorded ruling is provided in this example. A polished answer could leave you with the wrong result.</p></article><article class="wf-with"><span class="wf-kicker">WITH MOMM · GOVERNOR-VERIFIED EXAMPLE</span><h4 data-wf-output-heading>A result with a reason to trust the change.</h4><div data-wf-success-output><pre><code>let total = 0;
for (let i = 0; i &lt; prices.length; i++) {
  total += prices[i];
}
return total;</code></pre><div class="wf-result">Actual fixed output: <strong>${f.fixed}</strong><br>Expected: ${f.expected} · test passes</div><ul><li>Boundary claim reproduced; governor changed <code>&lt;=</code> to <code>&lt;</code>.</li><li>Additional empty-list check: <code>${f.empty}</code>.</li><li>Review participation, decisions and checks are inspectable.</li><li>You still decide whether to publish.</li></ul></div><div data-wf-blocked-output hidden><div class="wf-result wf-result-warning"><strong>BLOCKED: 1 of 2 required reviews</strong><br>No completed review gate. No verified-fix claim.</div><p>“One route completed; another was unavailable and another response was invalid. The required minimum was not met. Restore an eligible route or retry later.”</p></div><p class="wf-rejected" data-wf-rejected hidden>Rejected claim: “mutates input.” The fixture leaves its input unchanged. Disagreement is resolved by investigation, not headcount.</p></article></div>
 <details class="wf-text"><summary>Read the full workflow, reviewer roles and limits</summary><ol>${steps.map(s=>`<li><h3>${s.title}</h3><p><strong>${s.actor}.</strong> ${s.detail}</p></li>`).join('')}</ol><h3>Who can be the governor?</h3><p>The harness you are working in stays responsible. Switching the demo selector changes the illustration only; it does not sign in, install software or dispatch a review.</p><h3>How the reviewers differ</h3><ul>${routes.map(r=>`<li><strong>${r.name} · ${r.angle}:</strong> ${r.description}</li>`).join('')}</ul><p>Gemini CLI is an opt-in reviewer where account eligibility permits. Route status and actual media support must be checked; a product name is not evidence of readiness. Reviewer controls are not a complete operating-system sandbox.</p><p>For large split reviews, coverage and the required minimum must be checked for each relevant piece. Source changes, unresolved findings or incomplete verification prevent an honest completion claim. Historical reviews never prove a different commit is ready.</p><p>Automatic updates are off by default; no step here enables them. This illustration neither installs MOMM nor releases your work.</p></details>
 <div class="actions"><a class="button" href="technical.html">Full technical paper →</a><a class="button" href="evidence.html#real-review">Inspect a real review →</a><a class="button primary" href="start.html">Try MOMM on your change →</a></div>
 <p class="film-provenance">Workflow based on the <a href="https://github.com/marroccofella/skills/blob/momm-1.16.0/momm/SKILL.md">MOMM 1.16.0 protocol</a>. Fixture results are deterministic JavaScript, not attributed model outputs. The architecture guide retains all seven diagrams with source links and limitations.</p></section>`;
}
