// Public pages only. No private ledger, machine inventory or provider output.
import {runtimeLabel} from './momm-site-videos.mjs';
const esc = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const navigationLinks=[['index.html','Overview'],['install.html','Install'],['start.html','Get started'],['media.html','Media'],['evidence.html','Evidence'],['technical.html','Architecture'],['improvement.html','Improve MOMM'],['updates.html','Update safely'],['reference.html','Reference'],['releases/index.html','Versions']];
export function mediaBody(tour, films, version) {
  const seen = new Set(), ids=new Set();
  const items = [tour, ...films].filter(f => {
    if (f.status !== 'accepted') return false;
    if(f.schema!=='momm-tour/2'||!['overview','setup','trailer'].includes(f.id))throw Error('Unknown accepted film');
    if(!/^[a-f0-9]{64}$/.test(f.video_sha256||''))throw Error('Accepted film needs a valid hash');
    if(ids.has(f.id))throw Error('Duplicate accepted film identity');
    if(seen.has(f.video_sha256))return false;
    seen.add(f.video_sha256); ids.add(f.id); return true;
  });
  const categories = [['overview','Start here','The full introduction'], ['setup','Get connected','A focused setup walkthrough'], ['trailer','The short version','A brief introduction; overlaps with the full film']];
  return `<section class="page-hero"><p class="eyebrow">WATCH · LEARN · CHECK</p><h1>The MOMM media gallery.</h1><p class="lead">Choose the explanation you need. These are different edits, not extra reviews or independent evidence.</p><p>Current checkout release: ${esc(version)}. Recording versions below stay attached to their original films. Read the <a href="releases/index.html">release notes</a> for changes since recording.</p></section><div class="doc-body wide">${categories.map(([id,heading,description]) => {
    const f=items.find(item=>item.id===id); if(!f)return '';
    const folder=id==='overview'?'tour':`films/${id}`;
    return `<section aria-labelledby="media-${id}"><h2 id="media-${id}">${heading}</h2><div class="cinema-feature"><a class="chapter-frame" href="watch/${id}.html" aria-label="Watch ${esc(f.title)}"><img src="${folder}/poster.jpg" width="1280" height="720" loading="lazy" alt="${esc(f.title)}"><span class="chapter-play" aria-hidden="true">▶</span><span class="chapter-time">${esc(runtimeLabel(f.duration_seconds))}</span></a><div class="cinema-copy"><p class="eyebrow">${description}</p><h3>${esc(f.title)}</h3><p>${esc(f.description)}</p><p class="film-provenance">MOMM ${esc(f.version)} · recorded ${esc(f.recorded_date)} · consented synthetic Dom voice, ${esc(f.narration_speed)}×</p><a class="button" href="watch/${id}.html">Watch, captions &amp; transcript →</a></div></div></section>`;
  }).join('')}<aside class="notice"><strong>Approval is separate from production.</strong><p>Only films marked accepted in the published media catalogue appear here. Unfinished replacement films are not recommendations. Playback is voluntary, with native controls on each watch page; reduced-motion preferences do not hide videos.</p></aside></div>`;
}
export function improvementBody() {
  return `<section class="page-hero"><p class="eyebrow">IMPROVE THE WORKFLOW · KEEP THE HUMAN GATE</p><h1>Better through evidence.<br><span>Not automatic self-rewriting.</span></h1><p class="lead">MOMM can review changes to MOMM. That does not give a reviewer permission to change its instructions, publish private work or release software.</p></section><div class="doc-body wide"><section><h2>The improvement loop</h2><ol><li><strong>Observe:</strong> a release check records public version and asset metadata, tied to an exact commit.</li><li><strong>Suggest:</strong> observations go to GitHub Issues under a release-specific MOMM title. Unchanged reruns do not add comments.</li><li><strong>Investigate:</strong> an engineer reproduces a suspected defect and writes a regression test. A model's agreement is not proof.</li><li><strong>Decide:</strong> the governor records applied, rejected or deferred, with a reason and evidence.</li><li><strong>Review and release:</strong> an isolated candidate passes tests and peer review; a human authorizes publication.</li></ol><p><a class="button primary" href="https://github.com/marroccofella/skills/issues?q=is%3Aissue+%22MOMM+release+observations%22">Release observations on GitHub →</a> <a class="button" href="https://github.com/marroccofella/skills/issues/new?template=momm-observation.yml">Report an observation →</a></p><p>The proposed repository workflow runs on future stable MOMM releases or a maintainer's manual request once merged to the default branch. It posts bounded public metadata only. This page is not proof that the workflow has run; follow the linked issue and Actions run.</p></section><section><h2>What never happens automatically</h2><p>No model calls, capability re-probes, private ledger uploads, code fixes, merges, instruction changes, skill updates or releases. The observer does not mark a release READY. Closed observation issues stay closed; changed observations are bounded to one summary update, not repeated model suggestions.</p></section><section><h2>1.16.1 and 1.17 are different work</h2><p><strong>1.16.1:</strong> the draft patch line addresses proof and safety gaps, including installation inventory, source-bound receipts and clear dirty-clone recovery. Check <a href="https://github.com/marroccofella/skills/pull/18">draft PR #18</a> for its current scope and tests; a draft feature is not a stable release.</p><p><strong>1.17:</strong> candidate ideas include measured reviewer quality, bounded context, adaptive scheduling and installation management. They need acceptance criteria and an approved release plan. They are not promised features. <a href="https://github.com/marroccofella/skills/blob/1fc113fe7e8d62c2770fa0dd06afa238bc504f6d/momm/references/ideas-register.md">Read the ideas register →</a></p><p><a href="https://github.com/marroccofella/skills/blob/main/docs/momm/improvement-plan.md">Engineering record: findings, scope and next checks →</a></p></section><section><h2>Can a reviewer actually inspect your attachment?</h2><p>Use the shipped adapter limits, current capability evidence, the actual workload and the runtime result together. Vendor branding, model names and extensions do not prove perception. Local preflight makes zero model calls; active synthetic media probes are a separate, consented step. An expired or failed capability check must not count toward a media quorum.</p><p>A text-only reviewer cannot dismiss a visual finding by saying the source looks fine. Investigate the visual claim with the matching evidence; do not turn either model's opinion into an automatic blocker or approval.</p></section><section><h2>Send a useful observation</h2><p>Give the version and full commit, the smallest non-private reproduction, expected and actual behavior, and the relevant trimmed output. Remove tokens, account identifiers and local paths. Do not upload your private review ledger. For a security-sensitive report, use the repository's private reporting route if available; do not disclose an exploit publicly.</p></section></div>`;
}
export function normalizeNavigation(output) {
  for(const file of Object.keys(output).filter(f=>f.startsWith('docs/momm/')&&f.endsWith('.html'))){
    const relative=file.slice('docs/momm/'.length), prefix='../'.repeat(relative.split('/').length-1);
    const pattern=/<nav aria-label="(?:Main|Release) navigation">[\s\S]*?<\/nav>/g;
    if([...output[file].matchAll(pattern)].length!==1)throw Error('Expected exactly one primary navigation: '+file);
    output[file]=output[file].replace(pattern, '<nav aria-label="Main navigation">'+navigationLinks.map(([href,label])=>`<a href="${prefix}${href}"${href===relative?' aria-current="page"':href==='media.html'&&relative.startsWith('watch/')?' aria-current="true"':''}>${label}</a>`).join('')+'</nav>');
  }
}
